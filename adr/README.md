# Architecture Decision Records

Decisions are recorded as [MADR](https://adr.github.io/madr/) records: context, considered options, decision, and consequences both good and bad.

**The value is the rejected alternatives and the reasoning**, not the decision itself.
That is what stops the same argument happening again in six months.

## Index

| # | Decision | In one line |
| --- | --- | --- |
| [0001](0001-git-as-source-of-truth.md) | Git is the source of truth, index keyed by ref | Entities are markdown in the repos they describe; the index is derived and rebuildable, and `reconcile(ref)` is what makes PR previews nearly free |
| [0002](0002-plugin-protocol.md) | Plugins are subprocesses with a published contract | The subprocess boundary stands, as do the rejections of in-process, `hashicorp/go-plugin` and WASM. Transport superseded by 0039 |
| [0003](0003-license.md) | Apache 2.0 with a DCO | Patent grant for corporate adopters; DCO rather than a CLA because relicensing optionality is the thing being given up on purpose |
| [0004](0004-dusk-md-convention.md) | A repo opts in by containing `dusk.md` | One well-known file is the sole entry point. No repo is crawled without consent, and write routing falls out for free |
| [0005](0005-github-app-and-access-modes.md) | GitHub App via the manifest flow, three access modes | Dusk registers its own App so there is no PAT path; read, proposal, and write are the modes |
| [0006](0006-reconcile-triggering.md) | Webhook-triggered reconcile with a poll floor | The poll floor is load-bearing and must not be removed as redundant: webhooks are lost in normal operation and a pure-push system goes silently stale |
| [0007](0007-entity-schema.md) | Small schema, open kinds, attributes escape hatch | Entity, Relation, Note, Observation. Declaration and observation are different layers, and divergence is drift rather than conflict |
| [0008](0008-storage.md) | SQLite via GORM on a pure-Go driver | Refs as a column, FTS5 for search. The driver choice is load-bearing: the default GORM driver requires cgo |
| [0009](0009-proof-tokens.md) | Proof tokens gate every write | You cannot write what you have not read. One invariant replaces dedup, optimistic concurrency, and the vocabulary gate. Amended: a create is gated by whichever read could have witnessed the absence, and the call a rejection names is supplied by the write path rather than derived from the ref |
| [0010](0010-mcp-surface.md) | A few fat MCP tools, not a schema mirror | Seven tools, note kinds, and three layers of note dedup. Amended: commits go through the API, write mode commits straight to the default branch, and `get` is fat but bounded while `kind` narrows the search query rather than its answer |
| [0011](0011-ingester-scheduling.md) | Scheduling, shared API budget, never delete on failure | A failing ingester marks observations stale and never removes entities. "I could not look" is never "it is not there" |
| [0012](0012-viewing-auth.md) | Viewing authorization derives from repo access | No second permission model to drift. Per-entity filtering falls out for free |
| [0013](0013-layout-and-pages.md) | Pages are markdown, blocks are queries | Entity pages belong to satellite repos, portal pages to the config repo. Route collisions are impossible by construction |
| [0014](0014-agent-context-injection.md) | Context injected three ways, scoped to Dusk | MCP `instructions` is emitted before roots arrive, so it is location-blind; `dusk_context` and a hook cover the rest. Amended: the hook is a binary on `SessionStart`, the one event whose output reaches the model, it passes the answer through unchanged, and every failure of it is silent |
| [0015](0015-plugin-actions-and-events.md) | Entity-scoped actions, events not notes | Default deny, required dry run, proof tokens on mutations. This is where Dusk becomes a privileged control plane |
| [0016](0016-plugin-sdk-repo.md) | The plugin contract lives in its own repo | The `.proto` first, the Go SDK second, so non-Go authors are first-class |
| [0017](0017-engineering-policy.md) | Engineering policy for NerdsWhoFish repositories | Go conventions, no cgo without an ADR, package layout, DRY and its limit, docs everywhere, and the testing rules |
| [0018](0018-normalization-at-the-edge.md) | Plugins normalize, Dusk never re-derives | Data is final by the time it reaches the graph, which is what keeps renderers free of a branch per source |
| [0019](0019-chart-repo.md) | The Helm chart lives in its own repository | `NerdsWhoFish/charts`, published as OCI. Release coordination superseded by 0024 |
| [0020](0020-plugin-ui.md) | Plugins contribute UI as Web Components, in three tiers | Declarative spec by default, Web Component opt-in, iframe for the rest. Never compiled in, because that is the Backstage fork |
| [0021](0021-release-tooling.md) | Release with docker buildx and helm, not GoReleaser | Dusk ships an image and a chart, none of the artifacts GoReleaser exists for. All the release safety patterns are kept |
| [0022](0022-credential-encryption.md) | Credentials encrypted at rest with a required external key | No unencrypted mode. Envelope encryption so the key can rotate, and the chart never generates it |
| [0023](0023-plugin-configuration.md) | Plugins declare typed config; sensitive values never enter git | Non-sensitive config is markdown in the config repo, secrets are referenced from the encrypted store and are write-only |
| [0024](0024-charts-publishes-charts.md) | The charts repository publishes its own charts | Supersedes half of 0019. Chart version and appVersion are independent, so no App and no cross-repo write |
| [0025](0025-responsive-ui.md) | Mobile and desktop are both first class | A fixed viewport matrix is the definition, tests assert no overflow and touch target size, screenshot diffing is rejected |
| [0026](0026-dusk-md-schema.md) | One file declares one entity, and its prose is the description | Fills in what 0004 left open. Refs are derived not authored, only outbound relations are declarable, `include` is one level deep and cannot escape the repository. Also places the declaration path's normalization edge, which 0018 left unstated, and spends the repository's zero-dependency property on a YAML parser |
| [0027](0027-design-target.md) | The design target is a single operator and their agents | Homelabs and personal infrastructure, not platform teams. Team use stays supported but never breaks a tie. Corrects an audience assumption 0003, 0005 and 0013 had absorbed from Backstage |
| [0028](0028-amending-adrs.md) | ADRs are amended in place, and retired rather than deleted | Any part may be corrected where it stands, disclosed in a dated section; a wholesale rewrite supersedes instead. A dead decision is `Retired`, and no ADR is ever deleted |
| [0029](0029-reading-repositories.md) | Repositories are read over the API, at a pinned commit | Three endpoints instead of a clone, because a reconcile reads one file per repository. The ref is resolved once so a moving branch cannot stitch two commits into one graph, which also makes the tree cache safe |
| [0030](0030-account-allowlist.md) | Only allowlisted accounts are reconciled | Anyone able to see an App can install it, and catalog content reaches agent context, so an uninvited installation is a prompt injection path. Defaults to the App's own account, checked on both the sweep and the webhook |
| [0031](0031-notes-are-files.md) | A note is its own file, discovered like any other | Fills in what 0026 deferred. One file per note because notes are written most and contention lands there; the path is the id; a required discriminator says whether a file is a note or an entity |
| [0032](0032-tarball-reads.md) | Reads download a tarball, and only for repositories that opted in | Supersedes 0029's read half on the trigger 0029 itself named. Unchanged commit costs one call; a repository with no `dusk.md` is never downloaded; writes stay on the API |
| [0033](0033-graph-integrity.md) | The catalog reports what is wrong with itself | Duplicate declarations, dangling relations and unresolvable note refs become a read rather than staying silent. Reported, never rejected: an unresolvable ref is the normal state of a catalog still being adopted |
| [0034](0034-ingesters-in-tree-first.md) | Ingesters land in tree before the plugin protocol | The machinery, not the protocol, is the hard half. Observations get a reserved scope, a failed run never deletes, and declared beats observed. The plugin host becomes a second implementation of a proven interface |
| [0035](0035-blocks-resolve-server-side.md) | A page's blocks resolve on the server | Implements 0013. One request per page, one implementation of each block type shared with the agent renderer, and a failing block renders empty carrying its reason |
| [0036](0036-deriving-what-a-viewer-sees.md) | Signing in with GitHub, and what it does not protect | Implements 0012. Visibility derives from repository access, observed entities are hidden by default, invisible answers as absent. The shared token remains a full bypass, stated plainly |
| [0037](0037-pull-request-previews.md) | A pull request is a version of the catalog | Implements 0001's review half and finally uses 0008's ref-keyed index. Semantic diff, one comment edited in place, removals flagged, closing drops the ref |
| [0038](0038-what-drift-may-say.md) | Drift only speaks where something is watching | Extends 0011's rule that "I could not look" is not "it is not there" to kinds nobody observes. Coverage is derived from observations, and a declaration over its own observation stops reading as a conflict. Superseded by 0056, which keeps the witness rule and narrows the witness from the kind to the kind in its namespace |
| [0039](0039-one-plugin-transport.md) | gRPC is the only plugin transport | Supersedes 0002's transport half. The stdout tier cannot answer a request, so it cannot serve an asset or an action. `PluginService` already exists, so this deletes a tier rather than adding one. Shell script plugins are the cost |
| [0040](0040-core-and-plugins.md) | GitHub is core, Kubernetes is the first plugin | Core owns what Dusk cannot function without. Git is the source of truth so GitHub is substrate; Kubernetes is merely observed. The in-tree ingester goes only once the plugin replaces it |
| [0041](0041-plugins-reach-agents-as-actions.md) | A plugin reaches agents through the tools that already exist | Installing a plugin adds no MCP tools, or 0010's budget dies against an unbounded marketplace. Capabilities are 0015 actions surfaced through `invoke` and `get`; configuration is `declare`, and secrets stay UI-only |
| [0042](0042-installing-plugins.md) | Plugins are found by org, installed from a release, and cached on disk | The naming convention is the registry: `dusk-plugin-*` in an allowlisted GitHub org. Checksums verified, updates approved by a human, binaries on the PVC so boot needs no network. A plugin runs with Dusk's permissions, stated plainly |
| [0043](0043-plugin-configuration-stays-out-of-git.md) | Plugin configuration stays out of git, and an agent sets it through a tool | Amends 0023 and 0041. The frontmatter-in-git half of 0023 was never built and would make starting a plugin depend on the network, against 0042's offline boot. The sensitive half is sealed, which is the part that carried the security property. `configure` is one fixed tool, so 0041's constant surface holds |
| [0044](0044-plugins-keep-the-socket-directory.md) | Plugins keep the shared socket directory, and the token stays the boundary | A socketpair on an inherited descriptor would remove the path, and would re-decide 0039 by the back door: gRPC on a descriptor is comfortable only in Go. Per-plugin directories are theatre under one user, and unlinking after connect breaks reconnects |
| [0045](0045-drift-is-a-maintenance-queue.md) | Drift is a maintenance queue, not an inventory | Amends 0038. What is running and undeclared is reality reporting itself, not work, and on a real catalog it outnumbered the actionable half ten to one. Default is what was declared and no longer holds, plus notes pointing at nothing, which moves out of `integrity` so one rule has one owner. `DriftFilter{Undeclared: true}` restores the other direction. Amended: a note closed with status done or dropped leaves the queue, which the section had been telling readers to do while the query ignored it |
| [0046](0046-plugins-can-ask.md) | A plugin can ask the human a question, by returning it | `InvokeResponse.elicit` ends a turn and Dusk invokes again with the answer, rather than blocking inside one call. Returning the question is what lets a surface with nobody attached answer `unsupported` instead of hanging, so one declaration still serves an agent, the UI and a schedule. `Invoke` stays unary, so 0039 is not re-decided by the back door and existing plugins are untouched. Bounded at four turns |
| [0047](0047-a-form-holds-what-a-schema-declares.md) | A form holds what the schema declares, not what the type is called | The renderer had one control for everything that was not a boolean or an enum, so a declared array had no control at all and an entire shipped plugin could not be used from the browser. Control is derived from shape; `format` decides only how a string opens, and is free to declare because validators must ignore what they do not recognise. One renderer for parameters and for 0046's elicitation, because two would drift |
| [0048](0048-the-kind-vocabulary.md) | Kinds are derived, minted with a role, and never rejected | Fills in what 0010 sketched as `getKinds` and `mintKind`. The vocabulary is counted out of the index so it cannot go stale, and a mint adds only what derivation cannot reach: a role and aliases. Every role has a consequence, so nothing decorative can be minted; `reference` gives 0045 the precision it was blocked on. A near match warns and never refuses, because rejecting would delete 0007's open vocabulary. Also names the reserved paths in `.dusk/`, which a reconcile was failing on. Amended: the one refusal is a second spelling, not a name that exists |
| [0049](0049-a-notes-kind-is-its-rank.md) | A note's kind is its rank, through a role rather than a number | Implements the half of 0010 that was never built. Three roles, `warning`, `knowledge` and `work`, so a gotcha outranks a todo without being pinned and pinning goes back to meaning "this one specifically". Search demotes work rather than hiding or reweighting it. The role is also what decides whether a note can be closed, so `duskmd.Working` stops being a second list |
| [0050](0050-what-the-context-budget-buys-first.md) | What the context budget buys first | Implements 0014's pinning promise, which cost every session nothing because nothing pinned ever reached one. Sections are paid in priority order and printed in reading order, because written knowledge outranks enumerable fact: a ref left out is one `search` away and a gotcha left out is reachable by nothing. Relevance to the repository being worked in orders the pinned set without widening it. Nothing is dropped without being named, on room reserved before anything is spent, and a share cap stops one section silencing the rest. Amended: a section is charged what it printed, and the share binds on what is left ([0057](0057-charged-for-what-was-printed.md)) |
| [0051](0051-a-count-is-of-what-the-viewer-can-see.md) | A count is of what the viewer can see | Extends 0012 and 0036 to the three reads they left unfiltered: drift, integrity and the kind counts. A count that includes what a `get` would 404 for contradicts "invisible is indistinguishable from absent", and the difference between the count and the list is the hidden count exactly. A list is filtered where it is rendered, a count where it is computed, so `Visibility` becomes a required argument on every aggregate. Counting the whole estate, saying how much is hidden, and answering nothing at all were all rejected. Under the default observed setting a restricted viewer's drift is empty, which is correct and reads as broken |
| [0052](0052-a-write-that-cannot-land.md) | A write Dusk may not commit comes back as the change it would have made | Implements 0010's fourth rule, which nothing had. Dusk commits only in write mode; every other mode answers with a unified diff against the repository and path it would have written, through one gate so no write path can forget. Proposal mode was granted `contents: read` and could never commit either, so it gets the same answer without the branch and pull request being decided here |
| [0053](0053-note-dedup.md) | A note already written down is that note, and one nearly written is a warning | Implements 0010's other two dedup layers. The path is the body's hash, so an identical note is a file read away and comes back as its id rather than a 422; merging into it was rejected because 0009 exempts a create only because it cannot overwrite. A near duplicate is written and named, on 0033's reasoning, scored by word overlap over FTS5 candidates because a bm25 threshold means nothing between queries |
| [0054](0054-correcting-a-kinds-role.md) | A kind's role is corrected by minting it again | Amends 0048, whose one refusal was checked against the derived vocabulary and so caught the case it was written for: the role most worth correcting is the one nobody chose. The refusal narrows to a second *spelling*; the same spelling corrects the kind and keeps its aliases. The same tool, because the read that prints the current role is the one that issues the token, so a correction cannot be made blind. A distinct call and an optional role were both rejected |
| [0055](0055-supervising-plugin-processes.md) | A plugin whose process dies is restarted, and one that will not stay up is left failed | Pays the process lifecycle 0039 named and never built. The exit is the trigger rather than a poll; backoff is capped so a crash loop is not a hot loop and limited so it is not an infinite one; a process that stayed up resets the count, so "crashed once and came back" and "has never stayed up" are different states. Load-bearing: a dead or restarting process answers with an error and never with an empty observation, because 0011 makes empty a deletion. An interrupted mutation says its outcome is not known. Restarting on demand and an external supervisor were both rejected |
| [0056](0056-coverage-is-a-namespace.md) | Coverage is a namespace, and a row is not a verdict | Supersedes 0038 on the consequence 0038 wrote down itself. A kind being watched globally made every hand-written declaration read as missing the moment any plugin observed that kind anywhere: twenty one of twenty one rows on a real catalog were false positives, including the host the session was running on. A kind is now watched only in the namespaces it was observed in, and an `observed_as` counts as the operator supplying the witness, so narrowing does not silence the alias the mismatch case exists for. The report also stops asserting that a row means the thing is gone, names `observed_as` where the mismatch is read, and honours its own advice by clearing a note that has been closed |
| [0057](0057-charged-for-what-was-printed.md) | A section is charged what it printed, and the inventory leads with what is maintained | Fixes the allocation 0050 shipped, which answered a real catalog in 900 bytes of 8000: a section granted room for whole notes prints one-line shorts and keeps the difference, and a cap of half the budget does not bind when there are four sections. Spend is measured by rendering, and the share binds on what is left when a section's turn comes. Settles what 0050 did not: kinds rank by 0048's role, so infrastructure outranks reference and a service catalog stops eliding `service`; an overflow line names what it dropped rather than counting it; a heading is reserved beside that line; a ref reaching the index from two sources is listed once; and one sentence says the catalog acts as well as answers, because nothing else in the orientation mentioned `invoke`. Predicting the degraded size, redistributing after granting by want, and breadth-first rendering were all rejected |
| [0058](0058-a-read-time-is-a-fact-about-the-read.md) | A read time is a fact about the read, and it is stored with what was read | Makes 0011's claim that `observed_at` removes the need for separate freshness tracking true rather than aspirational: `changes` had no time on it at all. Derived from what is stored with the content, so it survives a restart, and it moves after a rebuild because the content was genuinely read again. A confirmed commit counts as a read; a failed one moves `Attempted` and never `At`. The retry schedule joins the answer, because "when is this corrected" is half of "is this current". Rendered relative and absolute, relative first, because an agent has no dependable sense of now. An in-memory time and a durable record of its own were both rejected |
| [0059](0059-what-a-list-may-not-leave-unsaid.md) | What a list may not leave unsaid | Applies 0050's rule to the rest of the surface, after driving it found four answers smaller than the truth. A filter narrows the query, never a page a limit already cut, which is what let a kind-filtered search report an absence it had not verified. Every list says "N of M", free for search through a window function and a second query for notes. A list too large to print whole names its tail rather than cutting it, which is what bounds `get` and amends 0010's "deliberately fat". A ref carries the name of what it points at; its status stays the plugin's, because Dusk would have to invent a schema to know which attribute that is. Amended: a read resolves its subject, because `neighbors` on a ref nothing declares answered exactly what a leaf answers |
| [0060](0060-finding-an-entity-by-part-of-its-name.md) | An entity's own name is matched by substring, and its prose is not | Infrastructure names are compounds and FTS5 matches words, so a search for three letters returned a person's surname and not the host whose name ends with them. An entity is now also found when its `name` holds every word of the query, ranked below a word hit and above a work note so 0049 still holds, with words under three characters left to the prefix match. A name is a handle rather than text about the thing, which is why prose keeps word semantics: substring over prose finds "cat" in "concatenate". Trigram-tokenizing the index, a second identity index, and matching the whole ref were all rejected, the last because a namespace fragment would return the whole catalog |
| [0061](0061-a-token-names-the-write-it-authorizes.md) | A token names the write it authorizes | Extends 0009 by applying its amendment to the offer rather than the refusal. One sentence offered every proof token to `declare` or `note`, which is false on a page read, on a note read, on a walk that covers one ref of the many it names, and on an empty search where "any of the above" is nothing at all. The read now names the write, which is what `kinds` had been doing privately all along. Deriving the offer from the token's origin was the close call and lost because the offer depends on what the read returned rather than on which read ran, and because a wrong derivation is invisible: the one being replaced was wrong on four reads for its whole life. A note read that matched nothing issues no token, since a new note needs none |

## Writing one

Number sequentially and never reuse a number.

Any part of an ADR may be amended in place, recorded in a dated `## Amendments` section at the bottom of the file saying what changed and why.
The limit is scale rather than category: a wholesale rewrite of the major sections is a **new** ADR that supersedes the old one, and the old one stays with its status updated.

A decision that no longer governs anything is marked **`Retired`**, with a dated entry saying what happened.
It keeps its number and its file. An ADR is never deleted, because its rejected alternatives are the reason nobody should re-propose what it rejected.

Full policy in [ADR-0028](0028-amending-adrs.md).

Every ADR states its rejected options and why they lost, and its consequences honestly, including the bad ones.
An ADR with no downsides listed has not been thought through.

Any rule an ADR states as load-bearing gets a test named after it, per [ADR-0017](0017-engineering-policy.md):

```text
TestADR0011_FailedIngestDoesNotDelete
```

**Adding, superseding, or retiring an ADR updates this index in the same change.**
An index that lies is worse than no index, because it sends readers to decisions that do not exist.
