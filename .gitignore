# `go build ./...`, which is what `make nocgo` runs, drops a main package's
# binary in the working directory.
/nextversion

/bin/
/dist/
/packages/browser-telemetry/node_modules/
/packages/browser-telemetry/*.tgz
